# SPEC：Breakpeek 轻讯息浮窗插件

| 文档属性 | 值 |
|---|---|
| 状态 | Draft（待实现） |
| 版本 | 0.3 |
| 更新日期 | 2026-09-06 |
| 适用范围 | Harness Web 轻讯息插件的产品需求、技术设计与验收规范 |

本文面向产品设计和插件开发，分析对象是 Breakpeek 独立项目；代码基线日期为 2026-09-03。第 1 节记录读取时的实现，后续章节均为待实现需求。产品名称为 **Breakpeek**，插件包名为 `@deepseek-ai/dsh-client-ui-breakpeek`，代码位于本项目根目录。

规范用语：“必须／不得”表示验收要求，“建议”表示允许在实施评审中调整的方案，“可选／后续”表示首期之外的能力。F 编号标识功能需求，A 编号标识验收场景；P0–P3 表示迭代阶段。

## 1. 现有插件实现分析

### 1.1 设计目的与实际行为

现有设计是在 Harness 页面中持续展示不依赖网络的短内容。它是页面内的 React 浮窗，通过 Cordis 插件安装，不是操作系统桌面窗口，也不具备浏览器关闭后的后台通知能力。

当前 [Breakpeek](src/client/Breakpeek.tsx) 不读取会话状态或静默时间。`visible` 直接表示浮窗的打开状态；浮窗关闭按钮与统一插件设置页读写同一字段。`autoRotate` 和 `rotationIntervalMs` 控制定时切换，前后箭头允许用户随时手动切换。当前版本没有详情、远程内容源或发送接口。

### 1.2 代码框架与职责

| 文件或入口 | 已有职责 | 后续处理方向 |
|---|---|---|
| [package.json](package.json) | 声明 Node、`./client`、`./invariant` 出口，以及 `dsh.client.platform = web` | 保留双入口插件结构，按新依赖调整声明 |
| [src/config.ts](src/config.ts)、[src/boot-config.ts](src/boot-config.ts) | 声明 `visible`、`autoRotate`、`rotationIntervalMs` Cordis schema、默认值与浏览器启动配置解析 | 随后续内容设置扩展 schema |
| [src/index.ts](src/index.ts) 的 `apply()` | 注册 `ui-breakpeek` settings namespace，以 Cordis 配置为默认层，并将解析值贡献为浏览器启动数据 | 随后续设置字段扩展 schema |
| [src/client/index.ts](src/client/index.ts) 的 `apply(ctx)` | 读取启动配置、绑定实时 settings scope，并注册浮窗及插件配置卡片 | 后续改为 root 浮窗注册，消除会话 slot 依赖 |
| [Breakpeek.tsx](src/client/Breakpeek.tsx) | 按显示和轮转设置管理内容索引、前后切换及关闭，并通过 Portal 渲染 | 拆出共享状态和阅读组件 |
| [BreakpeekSettingsCard.tsx](src/client/BreakpeekSettingsCard.tsx)、[settings-controller.ts](src/client/settings-controller.ts) | 在统一插件配置页中展示、暂存、保存及重置显示和轮转设置 | 随后续内容设置扩展控件 |
| [breakpeek-tips.ts](src/client/breakpeek-tips.ts) | 8 条中文静态内容，仅有 `face` 和 `text` | 建立有稳定 ID、摘要和正文的内容模型 |
| [Breakpeek.module.css](src/client/Breakpeek.module.css) | 右下角定位、布局、外观与入场动画 | 转为摘要／详情两种布局及完整主题 token |
| [src/invariant.ts](src/invariant.ts) | 注册空 invariant installer，理由是没有跨插件可变数据 | 实施后重新判断是否存在需要验证的数据关系 |
| [组件测试](tests/breakpeek.client.spec.tsx) | 描述显示开关、手动前后切换、定时轮转及点击关闭 | 补充注册释放与真实组装验证 |
| [tsconfig.json](tsconfig.json)、[tsdown.config.ts](tsdown.config.ts)、[build/client-bundle.ts](build/client-bundle.ts) | 包内自包含的 TypeScript 配置与 lazy-CJS Client 构建 | 不依赖仓库共享构建预设，确保浏览器实际加载更新后的 bundle |

宿主接入由安装该包的 DSH Profile 配置负责。`dsh.client.inject` 是包依赖信息，不负责运行时先后顺序；服务等待由 Cordis `inject` 完成，slot 声明等待由 `ctx.slots.inject()` 完成。

### 1.3 加载、数据与渲染链路

```text
Web profile / bundle 挂载插件
  → Node Config 校验 + apply() 注入 __DSH_BREAKPEEK_CONFIG__
  → manifest 的 ./client 出口
  → Client apply(ctx) 解析启动配置
  → settingsScope.bind('ui-breakpeek') 订阅实时用户设置
  → slots.inject('conversation.input.overlay')
  → slots.register(..., Breakpeek)
  → 读取 visible / autoRotate / rotationIntervalMs
  → 组件内部索引与轮转计时器
  → createPortal(..., document.querySelector('[data-shell-overlay]'))
  → AppFrame 的页面浮层

Client apply(ctx)
  → slots.inject('settings.plugin.item')
  → 在“插件配置”标签页注册 Breakpeek 卡片
  → 用户暂存并保存显示和轮转设置
  → Host settings 文档更新
  → settings scope 发布新快照
  → 当前浮窗立即采用新模式
```

组件借会话级 slot 获得运行时 props，但将 DOM 放到 Harness AppFrame 的浮层，因此不占输入框布局。该层覆盖整个应用框架，父层 `pointer-events: none`，直接子元素恢复交互。当前插件没有直接注册 `shell.overlay`，而是依赖 DOM 属性查找；容器不存在时返回空，也没有专门监听容器稍后出现。

### 1.4 时间、状态与视觉细节

| 项目 | 代码中的机制 | 局限 |
|---|---|---|
| 展示状态 | `visible` 默认为 `true`；关闭按钮写入 `false` | Cordis 值是部署默认层；可视化设置保存后作为用户覆盖层即时生效 |
| 自动轮转 | `autoRotate` 默认为 `true`；仅在浮窗可见时创建计时器 | 关闭自动轮转不影响手动切换 |
| 轮转间隔 | `rotationIntervalMs` 默认为 7000ms，允许 1000–3600000ms | 浏览器启动数据与设置 schema 同时验证范围 |
| 手动切换 | 前后箭头递增或递减索引，并在内容数组两端循环 | 尚无已读、去重和详情阅读锁定 |
| 手动关闭 | 立即隐藏，并通过 settings scope 持久化 `visible: false` | 写入失败时恢复显示，避免界面状态与已确认配置长期不一致 |
| 位置与大小 | 绝对定位，距右／下 24px，最大宽度 340px | 尚无展开尺寸、窄屏和遮挡规则 |
| 动画与外观 | 180ms 淡入、向上位移 6px；emoji 和中文文本 | 存在硬编码颜色及阴影，未覆盖减少动态效果偏好 |
| 可访问性 | 容器使用 `role="status"`、`aria-live="polite"`；关闭按钮有名称 | 缺少详情按钮、展开语义及焦点管理 |

组件计时器有 `clearInterval` 清理；插件不调用 Remote、不读写业务 store、不修改会话日志、不请求模型。当前本地内容不增加模型 token 或 KV Cache 成本。

### 1.5 验证基线

组件测试使用假时钟和可访问角色描述显示开关、前后切换、配置频率轮转及点击关闭。完整验证结果以当前变更实际运行的检查为准。

`invariant.ts` 的注释提及 HMR 释放验证，但所读取的测试文件中没有插件 fiber 卸载测试；现有证据包含组件与配置测试代码。本次已修改运行代码；按照本文第 10 节约束，获得用户许可前不执行自动化测试、构建、文档检查或真实浏览器验收。

## 2. 产品定位与范围

Breakpeek 是嵌入 Harness Web 界面的轻讯息投递与阅读插件。它在合适时机展示一条简短信息，用户可关闭，也可在同一浮窗内展开阅读全文。去除宠物、养成、陪伴角色及动物形象，以内容类别、标题和阅读动作作为界面核心。

本文将“发送”定义为内容源向插件投递讯息，再由插件按用户规则展示；首期生产者是本地内容池。“发送”不包含用户互发消息、向聊天发送问题或触发 agent。若后续需要向主会话提问，应作为独立需求，明确用户动作与会话日志行为。

| 场景 | 摘要态 | 原位展开后 |
|---|---|---|
| 面试练习 | 分类、题目、查看答案 | 答案、解释、代码示例；不自动替用户向模型提问 |
| 新闻阅读 | 标题、摘要、来源与发布时间 | 可取得的正文及来源信息；只有摘要时明确标注 |
| 技术提示 | 标题和一句摘要 | 解释、示例及适用条件 |

首期必须包含至少一个完整面试题和一个新闻类型示例，新闻示例明确标为演示内容。实时新闻获取放在后续内容源迭代，不把静态文本包装成实时资讯。

产品依附已有插件安装、启停与设置体系。页面关闭后不继续投递；不增加独立 app、系统托盘或操作系统通知。详情打开和关闭都不改变当前路由、会话和浏览器标签页。

## 3. 功能与交互需求

### 3.1 浮窗与阅读

| 编号 | 需求 | 可观察结果 |
|---|---|---|
| F01 | 摘要浮窗展示类别、标题、简短摘要、展开、关闭、设置入口 | 内容易辨识；标题过长时截断，完整内容在展开态阅读 |
| F02 | 点击标题或“查看详情／答案”原位展开 | 当前浮窗变大，保留锚点；URL 不变，不打开侧栏或新对话框 |
| F03 | 详情态展示完整内容并提供收起、关闭 | 长内容只在浮窗正文区域滚动，页面布局不移动 |
| F04 | 展开后锁定当前内容与滚动位置 | 轮换计时和新内容到达都不替换正在阅读的正文 |
| F05 | 点击关闭立即隐藏并保存关闭状态 | 配置页中的显示开关同步关闭；用户从配置页重新开启后恢复浮窗 |
| F06 | 设置页提供“立即查看一条”和“预览” | 关闭后仍能主动打开；预览不消费队列、不标记已读 |
| F07 | 提供上一条和下一条，支持收起后继续阅读当前条目 | 用户主动切换才退出阅读锁定；候选为空时明确反馈 |
| F08 | 关闭插件展示开关后，浮窗及调度停止，设置页仍可打开 | 可从设置重新启用；Loader 卸载插件则连设置贡献一起释放 |
| F09 | Cordis 与可视化配置提供显示开关、自动轮转开关和轮转间隔 | 三个字段在当前页面即时生效，并按宿主能力持久化 |

首期布局建议值：摘要宽度上限约 340px；详情宽度上限约 520px、高度上限为可视区域的 70%。窄屏宽度不得超过可用视口减去边距，避开输入区、移动端键盘和安全区。以上为待视觉验收的初始参数，不是现有实现承诺。

浮窗应位于普通聊天内容之上、宿主模态设置或审批界面之下。摘要自动出现不抢输入焦点；展开由键盘或点击触发，并设置可理解的标题及 `aria-expanded`。Esc 先收起详情，再次 Esc 关闭；关闭后将焦点交还仍存在的触发控件，否则保持合理的页面焦点。仅摘要变化使用轻量 live announcement，正文不作为持续播报区域。

### 3.2 展示、轮转与关闭语义

浮窗只有打开和关闭两种展示资格，不读取会话运行状态、输出变化或静默时间。自动轮转只负责切换当前讯息，不负责决定浮窗何时出现。

| 事件或条件 | 摘要态行为 | 已展开行为 |
|---|---|---|
| `visible` 开启 | 立即显示当前条目 | 保持当前正文 |
| `visible` 关闭或点击关闭按钮 | 立即隐藏并停止轮转 | 同左，并关闭详情 |
| `autoRotate` 开启 | 按 `rotationIntervalMs` 切换下一条 | 暂停轮转，保持当前正文 |
| `autoRotate` 关闭 | 保持当前条目，可手动前后切换 | 保持当前正文 |
| 点击上一条／下一条 | 立即循环切换，并重新开始完整轮转间隔 | 退出阅读锁定后切换 |
| 页面隐藏 | 暂停轮转 | 保留内容和阅读位置 |
| 页面恢复可见 | 从完整间隔重新计时，不追赶错过的轮转 | 保持当前正文 |
| 插件卸载或内容被明确移除 | 关闭浮窗并停止对应工作 | 同左；内容失效时给出原因 |

优先级为：插件安装状态 → `visible` → 用户阅读锁定 → 手动前后切换 → 自动轮转 → 内容可用性。设置预览是显式的隔离操作。

状态建议建模为 `hidden`、`summary`、`expanded`，并单独保存当前条目、轮转设置和阅读记录。关闭状态由 `visible` 表达，不能与暂停自动轮转、已读或无内容混为一个布尔值。一次接收内容不等于已经展示，摘要展示不等于已读；首期在首次展开正文时标记本地已读。

## 4. 插件设置

当前已实现 Cordis 插件配置 `visible`、`autoRotate` 和 `rotationIntervalMs`，并在统一的**设置 → 插件 → 插件配置**标签页中注册 Breakpeek 卡片。Node 入口以 Cordis 值注册 `ui-breakpeek` settings namespace 的默认层；用户在卡片中保存的选择写入 Host 设置文档的用户层，并通过 client settings scope 在当前页面即时生效。卡片中的“恢复部署默认值”删除三个字段的用户层值，使配置重新继承 Cordis 值。插件安装／卸载仍归宿主插件管理。

| Cordis 配置项 | 默认值 | 当前规则 |
|---|---|---|
| `visible` | `true` | 表示当前浮窗打开状态；关闭按钮与可视化开关写同一字段 |
| `autoRotate` | `true` | 打开时按间隔自动显示下一条；关闭后保留手动前后切换 |
| `rotationIntervalMs` | `7000` | 自动轮转间隔，范围为 1000–3600000ms；可视化卡片以秒为单位编辑 |

| 配置项 | 首期建议 | 规则 |
|---|---|---|
| `categories` | 面试题、技术提示、演示新闻 | 可多选；全部不选时展示空状态，不静默换成其他类别 |
| `sourceIds` | 内置内容源 | 仅允许选择已注册且可用的来源 |
| `position` | 右下 | 首期固定右下；后续可选左右下角，受视口约束 |
| `pauseOnInteraction` | 后续默认开启 | 展开、悬停或键盘聚焦时暂停自动轮转 |

这里的默认值和范围是待实现的产品方案。时长、筛选及布局选择必须进入明确的设置 schema 或部署 Config，不能只用模块常量充当配置；对定时器内部精度等实现参数，仅在确有部署调整需要时公开。

当前卡片先暂存字段，用户点击保存后统一写入；保存期间禁用控件，失败时显示提示并采用宿主确认的值。初次设置加载完成前使用 Host 注入的启动配置。恢复默认值使用相同保存链路。预览采用本地草稿，不必先改变已保存偏好。

持久化沿用现有宿主 settings namespace 的作用域，不承诺逐会话偏好或跨设备账号同步。Harness SettingsScopeController 通过共享 mirror 派生快照、串行写入并使用 revision 防止覆盖。其写入失败会执行恢复读取，不能仅以 `set()` 的 Promise fulfilled 判定保存成功；实现时需依据确认后的值／版本判断结果，若不足以表达失败则补足所属设置 API 的结果语义。

当前 Harness 设置服务仅对 loopback 连接使用宿主持久化，远程浏览器为 memory 模式。轻讯息设置必须明确显示“仅本次页面生效”，在这一模式下仅更新插件的内存偏好，不直接调用受限的宿主写接口。需要远程持久化时另立宿主能力需求，不绕过既有权限。

## 5. 内容模型与投递

### 5.1 统一条目

| 字段 | 含义与约束 |
|---|---|
| `id` | 稳定条目 ID，跨包时使用 branded ID；不能以轮换数组下标充当身份 |
| `kind` | `interview / news / tip` 判别字段，决定正文含义和动作文案 |
| `title`、`summary` | 摘要展示；面试题的摘要不能提前泄露答案 |
| `body`、`bodyFormat` | 首期本地提供完整正文；可用纯文本或受限 Markdown |
| `sourceId`、`sourceLabel` | 来源身份与展示名称，合成去重键时考虑来源 |
| `publishedAt`、`expiresAt` | 新闻发布时间与可选过期时间；时间格式和时区在输入解析时验证 |
| `tags` | 内容筛选标签；不用于替代明确的类别 |
| `sourceUrl` | 可选来源引用，不作为打开详情的实现路径 |

用户已读、关闭记录、展示次数等属于用户状态，不回写公共内容条目。首期阅读状态仅保存在页面内存；刷新后可重置，后续有明确回看需求再增加有容量与保留期限的持久化历史。

### 5.2 投递规则

```text
内容生产者（首期为内置列表）
  → 输入解析／规范化
  → 按来源与 ID 去重、过滤过期内容
  → 按用户来源和类别筛选
  → 有容量限制的候选队列
  → 展示策略判断
  → 摘要浮窗
  → 用户展开并锁定同一条正文
```

首期从筛选后的本地池依次选择；同一轮候选遍历不重复，遍历完可重新开始，但跳过本次页面已关闭条目。池为空时自动模式安静等待，手动查看显示“暂无符合条件的内容”及设置入口。队列上限、单条正文大小和保留时间要由 owning provider 明确给出并验证，过期条目优先清理，队列满时淘汰最旧的未展示候选，不能替换正在阅读的条目。

后续远程来源通过宿主插件获取并返回结构化内容，客户端不持有抓取密钥。按需读取正文时提供加载、失败、重试状态；关闭、切换条目或卸载后取消请求，并用条目身份和请求代次阻止迟到响应覆盖当前内容。没有全文时只展示已取得的摘要及说明，不自动跳转来源网站。

远程内容按不可信输入解析：限制正文大小，禁用原始 HTML 和脚本 URL；代码块仅用于展示。它默认不进入模型上下文。将来若支持模型生成或“就此提问”，必须显式启用，并遵守仓库的模型可见内容日志要求。

## 6. 建议实现框架

### 6.1 插件内部职责

建议先在一个功能插件内完成 UI、本地内容与设置，不为尚不存在的消费者提前拆出服务包。待远程投递确有独立生产者与消费者，再按仓库要求组织 Service Definition、Service Provider、Consumer，避免只有 interface 的空扩展框架。

```text
src/
  index.ts                      Node: settings namespace / schema 注册
  breakpeek-settings.ts     共享的设置 schema 与默认值
  invariant.ts                  插件拥有的数据关系校验
  client/
    index.ts                    apply: 组装、注册、订阅与释放
    types.ts                    内容、展示状态和用户动作类型
    content.ts                  本地内容及输入规范化
    store.ts                    createBreakpeekStore 工厂
    controller.ts               内容选择、设置采用、调度和取消
    BreakpeekOverlay.tsx         摘要／详情浮窗
    BreakpeekSettings.tsx        设置分区和预览
    Breakpeek.module.css         主题 token、尺寸与动效
tests/
  ...                         按策略、交互、注册释放和真实组装划分
```

这是职责草图，不要求机械地产生所有文件；同一职责很小时可合并。跨包访问使用包名，组件 props 从 slot runtime、store 和 inject 推导；浏览器组件不直接持有完整 Cordis `ctx`。

### 6.2 注册、作用域与共享状态

浮窗后续直接贡献到 Harness 布局的 `shell.overlay` slot，采用 root scope，消除 DOM 查询、Portal 容器时序和会话组件挂载依赖。当前实现仍从 `conversation.input.overlay` 注册并通过 Portal 渲染，但不读取 `useSession` 或其他会话事实。设置卡片使用 root scope；迁移浮窗注册时，两个入口可共享在 `apply` 中创建的状态控制器，但同一个 hook handle 不得跨 scope 挂载。

共享状态保存当前条目、显示状态、轮转设置及阅读记录；正文内部滚动位置等由组件在条目身份稳定时维护。卸载、关闭展示和页面隐藏分别清理其拥有的计时和请求；所有注册及订阅随插件 fiber dispose，重新挂载不能产生重复浮窗或多重定时器。

### 6.3 设置和模块接入

当前 Node 入口使用 `Config` schema 校验 `visible`、`autoRotate` 和 `rotationIntervalMs`，将 Cordis 配置作为 `ui-breakpeek` settings namespace 的默认层，并在每次 `webserver/index-inject` 收集时贡献当前解析值 `__DSH_BREAKPEEK_CONFIG__`。浏览器入口把启动数据视为 wire 输入，验证字段类型与间隔范围后作为 settings 首次加载前的回退值；启动数据缺失时采用默认配置，格式错误时明确失败。settings scope 就绪后成为当前页面的值来源，因此可视化保存无需刷新即可改变显示与轮转行为。

浏览器在 `apply` 内分别绑定浮窗和配置卡片使用的 settings scope，避免跨 root/session scope 复用同一个 handle。卡片通过 Harness `ui-settings-plugins` 声明的 `settings.plugin.item` 扩展点进入统一插件配置标签页，并自行拥有控件、暂存、带 revision 的并发写入保护、失败反馈和双语文案，不依赖 DOM 查找私有设置结构，也不另造 localStorage 配置源。

浮窗齿轮需要宿主支持按分区打开设置。现有 `SettingsSectionOwnerProps` 仅含 `close`，`openSection` 出现在 onboarding props，不能直接假定浮窗能调用它。实施时先查验设置面板的公开动作；若没有精确入口，应由设置／布局 owning plugin 提供最小的打开分区动作并更新声明，而非点击 DOM 或依赖私有 store。

插件名称在包目录、manifest、Web bundle 行、bundle dependency、编译引用、组件和测试中统一使用 Breakpeek 对应标识。后续实现保持这些引用一致，并重建浏览器 bundle。

## 7. 迭代计划

| 阶段 | 交付内容 | 完成条件 | 依赖 |
|---|---|---|---|
| P0：可靠原型 | 持久化显示开关、自动轮转开关与频率、手动前后切换、关闭同步设置；恢复可信测试 | 显示／隐藏、保存／重置、手动切换和定时轮转可验证，卸载无残留 | 对本文关闭与轮转语义完成评审 |
| P1：首期可用版本 | 摘要与原位详情、本地完整内容、设置保存反馈和预览；完成 root 浮窗注册 | 面试题可看答案，新闻示例可读正文；设置刷新后按宿主能力恢复；展开期间暂停轮转 | P0，设置入口动作确定 |
| P2：多内容源 | 宿主新闻 provider、刷新与缓存、来源／类别筛选、去重过期、加载失败与取消；周期投递及免打扰 | 真实来源从获取到原位阅读全链路通过，断网及迟到响应行为明确 | P1；新闻源、凭据方式及正文可取得范围明确 |
| P3：阅读体验 | 有界已读历史／收藏／稍后读、位置偏好、可选排序 | 用户能主动回看；存储有容量与清理规则；多标签页重复策略明确 | P2 或独立的持久化回看需求 |

P0 和 P1 共同构成第一轮可发布目标；仅修正显示问题不能算完成本需求。P2 的远程来源和周期调度可分别交付，但都遵守阅读锁定及关闭语义。P3 不作为首期前置条件。

每阶段按“数据与策略 → 用户操作 → 插件组装 → 验证”推进。先以本地确定性内容证明阅读流程，再引入网络变量；首期不引入个性化推荐模型、全网爬虫、富文本编辑器或即时通讯能力。

## 8. 验收与测试计划

| 编号 | 场景与预期 | 验证层次 |
|---|---|---|
| A01 | `visible: true` 时立即展示，`false` 时隐藏；会话活动不改变结果 | 配置注入＋组件行为 |
| A02 | 点击关闭立即隐藏并保存 `visible: false`；配置页重新开启后恢复 | 设置与组件行为 |
| A02a | 自动轮转只在开关启用且浮窗可见时按配置间隔切换，关闭后不创建轮转计时器 | 假时钟＋组件行为 |
| A03 | 面试题展开显示答案，新闻展开显示正文；路由、会话、页面布局保持 | 真实 Web 组装与 keyless snapshot |
| A04 | 前后箭头循环切换；阅读期间自动轮转不替换正文；收起与 Esc 符合规则 | 组件交互＋浏览器 |
| A05 | 三项设置保存后即时生效，本机刷新后恢复；重置后继承 Cordis 默认值；远程内存模式有明确提示 | 设置 provider 集成＋浏览器 |
| A06 | 设置读取失败、写入失败、revision 冲突不会显示虚假的保存成功 | 设置集成，包含拒绝输入和失败路径 |
| A07 | 空类别、空池、重复、过期、超限内容得到明确处理 | 内容与 provider 测试 |
| A08 | 页面隐藏恢复不补发多条，卸载／重载不遗留注册、计时器和请求 | fiber dispose / HMR 测试＋浏览器 |
| A09 | 键盘、窄屏、长正文、主题及减少动态效果可用；不遮挡审批交互 | 浏览器交互与视觉检查 |
| A10 | 远程超时、重试、取消、迟到响应和不可信正文不破坏当前阅读 | P2 provider 与真实来源 e2e |

实施时更新 package README、JSDoc 和本提案的实现状态。按仓库 GUI 规则执行相关 GUI 测试、真实配置加载验证及 keyless Web snapshot；不能只靠手动 `ctx.plugin` 或组件 mock 宣称完整插件可用。GUI 行为 PR 还需真实服务流程录制的 GIF。新增 provider 时补充真实 API e2e；只有密钥不可用时按测试策略说明缺失的证据。

本文交付仅为需求和设计文档；上表为未来实现的验收门槛，不代表已通过。

## 9. 设计取舍与待定项

插件形式、原位展开、可关闭和可设置是明确要求。采用 root 浮窗，是为了让阅读与轮转不受输入框和会话重挂载支配；相关设计取舍记录在 [轻讯息提案](.agents/notes/proposed/feature/2026-09-03-light-message-reader.zh.md)。

实施前需要确定的产品项包括：首批内容范围与新闻来源、是否提供远程持久化，以及未来是否有用户主动向其他人“发送”的需求。它们不阻塞 P0/P1 的本地内容阅读流程；本方案按“Breakpeek、本地题库与演示新闻、可视化用户层覆盖 Cordis 默认层、遵循宿主持久化能力、仅向浮窗投递”的基线推进。

## 10. 开发验证约束

修改 Breakpeek 代码或文档后，可以直接执行 UI 验证，无需事先询问用户。这里的 UI 验证仅指在已有可运行界面上进行人工交互和视觉检查；若需要先执行构建、启动自动化 GUI 测试或调用其他受限验证命令，仍按下述规则处理。

自动化测试、客户端测试、构建、文档检查、GUI 测试及其他验证命令不得自动执行。实施者必须先列出建议运行的命令并询问用户，获得明确同意后方可执行。

Breakpeek 的 Host 与浏览器运行时分别加载 `lib/index.js` 和 `lib/client.js`。修改影响运行行为的源码后，实施者必须提示用户重新生成包级构建产物，列出建议命令（通常为项目根目录下的 `pnpm build`），并在执行前取得用户同意。构建完成前，UI 中仍可能运行旧产物，不得将该 UI 结果作为当前源码已经生效的证据。

Breakpeek 自行维护 `tsconfig.json` 和 `build/client-bundle.ts`。包级 `build` 命令必须在全新产物目录中完成 TypeScript 编译、Host 入口生成、CSS Modules 内联及 lazy-CJS Client 包装，不得隐式引用 DeepSeek Harness 仓库内的构建脚本。发布前必须保留 Profile 集成验证。

用户在任务中明确要求“修改后执行验证”或明确指定验证命令时，可以在完成修改后直接执行对应检查，无需再次询问。本约束不限制代码阅读、搜索、`git status`、diff 审阅等不会运行项目验证流程的只读检查。
